return {
  "folke/snacks.nvim",
  opts = {
    -- Disable auto-highlighting of the word/symbol under the cursor
    -- (the blue LSP "document highlight" of matching references).
    words = { enabled = false },
  },
}

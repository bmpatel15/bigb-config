# Raw stdout config for the dashboard audio visualizer (read by services/Cava.qml).
[general]
framerate = 30
bars = 14
autosens = 1

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 100
channels = mono
